using Gtk 4.0;
using Adw 1;

Gtk.FlowBox flow {
    homogeneous: true;

    column-spacing: 5;
    row-spacing: 5;
    // TODO: Use brakepoints
    max-children-per-line: 4;
    min-children-per-line: 1;
    selection-mode: none;
    hexpand: true;
    halign: center;
  }
